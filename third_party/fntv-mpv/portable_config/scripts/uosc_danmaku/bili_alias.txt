# B站 谐音/近似名 别名映射（最后兜底用）
# 用途：有些番 B站 没有官方上传，UP主用谐音梗名字（如「葬送的芙莉莲」→「早丧的福里莲」）
#       上传，常规标题匹配不到。在这里手动填一次映射，脚本会优先用「搜索词」去 B站 找，
#       能 100% 命中谐音名搬运；找不到时仍可走模糊猜测兜底。
#
# 格式：每行一条，等号左边填你本地文件的真实番名（或其中关键词），右边填 B站 上的搜索词。
#       等号左右都不要加引号，行首 # 为注释。
# 例：
# 葬送的芙莉莲=早丧的福里莲
# 某番真实名=某番谐音名
