node_modules
release
# third_party: 只放行文本/源码配置，所有二进制 / 缓存严禁入库（由构建时 go build 生成或 CI 下载，不必提交）
third_party/**
!third_party/**/
!third_party/**/.gitkip
!third_party/**/*.md
!third_party/**/*.txt
!third_party/**/*.conf
!third_party/**/*.bat
!third_party/**/*.ps1
!third_party/**/*.ico
!third_party/**/*.png
!third_party/**/*.webp
!third_party/**/*.glsl
!third_party/**/*.lua
!third_party/**/*.json
!third_party/**/*.js
!third_party/**/*.html
!third_party/**/*.py
package-lock.json
!.gitkip
*.zip
log
*.log
docs
dest
paly_Full.txt
77777.txt
play_video.py
*.exe
.idea
.vscode

# 用户自定义 mpv 配置：B站 登录凭证与本地备份严禁提交
bili_cookie.txt
*.bak*

# 个人播放数据 / 运行时产物：严禁提交
**/watch_later/
**/__pycache__/
danmaku-history.json
mpv.log
**/.cache/
**/cache/
mpv.conf.bak*

# 发布流程临时文件
.exeresp.json
.relbody.json
.relid.txt
.relresp.json
.ymlresp.json
_test_parse.js

# 截图 / 临时调试产物
shots/

# ⚠️ 本地 commit 归档：纯本地回退保底，【严禁推送 GitHub】
# 内含 commits.md（详细改动记录）与 项目记忆.md，仅本机使用，绝不入库。
本地commit/
本地commit/*

# ⚠️ 版本更新日志：由维护者本人手动上传到 GitHub Release，【严禁推送 GitHub】
# 命名约定 CHANGELOG_vX.Y.Z.md（如 CHANGELOG_v3.3.0.md），仅本地维护，不入库。
CHANGELOG*.md

# ⚠️ 根目录一键构建脚本（build-gui.js / 构建.cmd）：用户要求【严禁推送 GitHub】
# 仅本机双击使用，备份保存在 backup-before-rewrite 分支。
build-gui.js
构建.cmd
打包.cmd

# resource 软件资源目录：用户要求纳入 GitHub（放行 resource/docs 下图片/资源）
# 注意：resource/wiki 下 CHANGELOG_vX.Y.Z.md 仍按上方 CHANGELOG*.md 约定不入库（发 Release 用）
!resource/docs/
!resource/docs/**

# 热补丁统一目录 patches/：保留正式 hotfix，忽略测试补丁（含带 proxy 的超大临时产物）
patches/patch-*-test*.json

# third_party/potplayer 由 copy-potplayer.js 运行时从本机复制，不入库
third_party/potplayer/

# 根目录调试/验证临时产物（可重生成，不入库）
__v5_check.py
p5.json
test3_verify_report.html