# Fntv-Plus 上游代码借用分析报告

> 分析日期:2026-09-14
> 本项目:`YDMY007/Fntv-Plus`(v3.7.0,release 分支,1074 个提交)
> 上游:`QiaoKes/fntv-electron`(HEAD = release 分支,v2.6.2,2026-08-26,完整历史 321 个提交)
> 本项目于 2026-09-09 脱离上游 fork 网络,成为独立仓库。

## 一、结论摘要

1. **初始发布(v3.0.0,2026-07-21)基本是上游 v2.6.1(2026-04-12)的整体继承**:上游当时的 110 个源文件中有 102 个被原样带入,其中 77 个逐字节完全相同(含 51 个文本文件共 9355 行、26 个图标等二进制资源);只有 8 个上游文件未引入。按行数计,上游当时约 1.3 万行源码中约 85%~95% 进入了初始发布。
2. **经过约 1000 个提交的演进,当前项目中仍可追溯到上游的代码约 10200 行**,约占本项目 src/ 源码(58119 行)的 **17% 左右**;其余 80% 以上为自研或新增。
3. 借用集中在**基础设施层**:飞牛 API 封装(fn_api)、日志模块(logger)、播放器抽象(players)、Go 本地代理(proxy)、登录/认证(config/login/fnid_login/auth)、证书信任(cert_trust)以及登录页(login/index.html)。这些模块至今与上游相似度仍达 70%~98%。
4. **自研为主的层**:src/preload/plugins 下的全部 UI 注入与增强(embyWall 轮播墙、详情页美化、弹幕、Bangumi/TMDB/Trakt 同步、年度报告、手柄控制增强、安装向导 NSIS 画稿体系、三平台打包流程等)——这是本项目从"客户端壳"长成"增强版"的主体增量。
5. 许可证方面,上游与本项目均为 **GPL-3.0**,代码继承在许可证框架内;脱离 fork 网络不影响 GPL 义务(保留版权声明、以同许可证开源)。

## 二、fork 点对比(初始发布 vs 当时上游)

对比对象:本项目初始提交 `95e3be7`(v3.0.0,2026-07-21) vs 上游 `6fe15d4`(v2.6.1,2026-04-12,fork 点上游最新提交)。

| 指标 | 数值 |
|---|---|
| 上游文件数(排除构建产物) | 110 |
| 本项目初始文件数 | 343 |
| 两边共有文件 | 102 |
| 内容逐字节相同 | **77**(51 个文本文件 9355 行 + 26 个二进制/图标) |
| 有差异的文本文件 | 23 |
| 仅上游有(未引入) | 8(resource/docs 文档、afdian 赞助配置等) |
| 仅本项目初始有 | 241 |

初始发布时 23 个有差异文件的相似度分布:

| 相似度 | 文件数 | 说明 |
|---|---|---|
| ≥95% | 7 | media.ts(99.8%)、fn_api/request.ts(99.4%)、main.ts(99.3%)、package.json、updateChecker.ts、playMaskButton.ts、players/impl/mpv.ts |
| 80%~95% | 3 | proxy/main.go、wincontrol.ts、constants.ts |
| 50%~80% | 2 | — |
| <50% | 11 | README、mainwin.ts、titlebar.ts、preferences.ts、mpvConfig.ts、cert_trust、部分 icon 脚本等(初始即已大改或重写) |

仅本项目初始就有的 241 个文件中,235 个属于 `third_party/fntv-mpv`(内嵌 mpv 播放器运行时:mpv.exe、d3dcompiler_43.dll、portable_config 等,属第三方发行包,非上游代码),真正的自研源码增量当时还很少——即 **v3.0.0 ≈ 上游 v2.6.1 + 内嵌 mpv + 少量定制**。

## 三、当前状态对比(现在 vs 上游最新)

对比对象:当前工作区(v3.7.0) vs 上游 HEAD(v2.6.2,2026-08-26),均排除构建产物。

| 指标 | 数值 |
|---|---|
| 两边共有文件 | 102 |
| 逐字节完全相同 | 48(22 个文本文件 2132 行 + 26 个二进制) |
| 有差异的文本文件 | 51 |
| 仅本项目有(自研/新增) | 文本 276 个(不含构建产物),另有构建产物/二进制资源 |
| 仅上游有 | 201(上游 8~9 月新增的访问码登录、macOS 原生控件等,本项目未跟进) |

51 个有差异文件的相似度分布:

| 相似度 | 文件数 | 代表文件 |
|---|---|---|
| ≥95% | 3 | fn_api/types.ts(97.9%)、players/index.ts(96.0%)、logger/maskingConfig.ts(95.8%) |
| 80%~95% | 17 | login/index.html、config.ts、login.ts、cookie.ts、proxy 内多数 Go 文件、api.ts 等 |
| 50%~80% | 14 | proxy/main.go、winctrl、update.ts、handlers/index.ts、tray.ts 等 |
| <50% | 17 | mainwin.ts(6.0%)、titlebar.ts(11.6%)、playButton.ts、package.json(3.9%)、fn_config/config.ts(0%)等(已基本重写) |

**当前仍与上游逐行一致(LCS 公共行)的代码估算:约 10200 行。**

## 四、借用/自研模块分布

### 仍以上游代码为主(相似度 70%~98%,即"借用层")

| 模块 | 当前相似度概况 |
|---|---|
| src/modules/fn_api(飞牛 API/请求封装) | 70%~98% |
| src/modules/logger(日志体系) | 72%~96% |
| src/modules/players(播放器抽象) | 71%~96% |
| src/modules/proxy(Go 本地流媒体代理) | 70%~90% |
| src/main/handlers/plugins:config / login / auth / fnid_login / clean / update | 40%~92% |
| src/modules/fn_config(账号/Cookie) | 90% / 0% |
| src/modules/cert_trust(证书信任) | 91% |
| src/preload/core(hooks/types/logger/utils) | 65%~88% |
| resource/login/index.html(登录页) | 92% |

### 本项目自研/大幅重写(上游无对应或相似度 <50%)

- src/preload/plugins 全部 UI 增强:embyWall(轮播墙/详情页/剧集回填)、titlebar、playButton、playMaskButton 等(约 2.8 万行)
- Bangumi / TMDB / Trakt 同步、B 站弹幕、年度观影报告、演员作品库
- third_party/proxy 的 potctl 及各平台 Go 构建矩阵
- 安装向导 NSIS 画稿体系(scripts/gen-nsis-art、installer.nsh、build/)
- 三平台打包流水线、深色模式适配、镜像源更新检查等

## 五、规模与占比

| 口径 | 行数 |
|---|---|
| 本项目 src/ 源码(ts/js/go/html/css) | 58,119 行 |
| 上游全部源码(同口径) | 13,906 行 |
| 当前仍可追溯为上游的代码 | ≈ 10,200 行 |
| 上游代码占本项目源码比例 | **≈ 17%**(10,200 / 58,119) |
| 初始发布时占比 | 当时项目源码绝大部分来自上游(除 third_party/fntv-mpv 与少量定制外) |

## 六、合规提示

- 双方均为 GPL-3.0:继承上游代码合法,但需继续保留上游版权与许可证声明,并保持同许可证开源(当前仓库 LICENSE 与 README 已覆盖,建议在"关于/致谢"中明确致谢 QiaoKes/fntv-electron)。
- 上游 8~9 月新增的功能(访问码登录、macOS 原生窗口控件等)本项目未引入,不存在该部分借用。

---

*分析脚本:`scripts/_compare_upstream.cjs`(当前全量对比)、`scripts/_compare_forkpoint.cjs`(fork 点对比)、`scripts/_compare_current_sim.cjs`(当前相似度明细)、`scripts/_borrowed_lines.cjs`(LCS 行数估算)。上游克隆于 `C:/Users/24305/AppData/Local/Temp/fntv-upstream`(经镜像加速,完整历史)。*
